{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "boot": "lerna bootstrap",
    "start": "lerna run start",
    "deploy": "lerna run deploy"
  }
}
