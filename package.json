{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "boot": "lerna bootstrap",
    "start": "lerna run start --stream --progress",
    "deploy": "lerna run deploy",
    "format": "lerna run format"
  },
  "dependencies": {}
}
